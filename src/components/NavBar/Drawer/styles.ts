import { styled } from "@mui/system";

export const Logo = styled("div")(({ theme }) => ({
	display: "flex",
	paddingTop: "10px",
	alignItems: "center",
	// [theme.breakpoints.down("md")]: {
	// 	padding: "15px",
	// },
}));
